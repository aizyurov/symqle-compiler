public interface test_interface<T>
{
    void dump(OutputStream os, boolean compress);
}

public class TestClass<T> implements test_interface<T>
{
    public Integer dump(OutputStream os, boolean compress) {
        // do nothing
    }
}
